function alphabetOrder(str) {
    return str.split('').sort((a, b) => a.toLowerCase().localeCompare(b.toLowerCase())).join('');
}

let exampleString = 'Webmaster';
console.log(alphabetOrder(exampleString)); // Output: 'abeemrstw'
